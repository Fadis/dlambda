#ifndef DLAMBDA_TYPE_TRAITS_IS_ENUM_HPP
#define DLAMBDA_TYPE_TRAITS_IS_ENUM_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    bool is_enum( const type& );
  }
}

#endif

