#ifndef DLAMBDA_TYPE_TRAITS_IS_SAME_HPP
#define DLAMBDA_TYPE_TRAITS_IS_SAME_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    bool is_same( const type&, const type& );
  }
}

#endif

