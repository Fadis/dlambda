#ifndef DLAMBDA_TYPE_TRAITS_REMOVE_VOLATILE_HPP
#define DLAMBDA_TYPE_TRAITS_REMOVE_VOLATILE_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    type remove_volatile( const type& );
  }
}

#endif

