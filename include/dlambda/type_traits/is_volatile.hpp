#ifndef DLAMBDA_TYPE_TRAITS_IS_VOLATILE_HPP
#define DLAMBDA_TYPE_TRAITS_IS_VOLATILE_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    bool is_volatile( const type& );
  }
}

#endif

