#ifndef DLAMBDA_TYPE_TRAITS_IS_SIGNED_HPP
#define DLAMBDA_TYPE_TRAITS_IS_SIGNED_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    bool is_signed( const type& );
  }
}

#endif

