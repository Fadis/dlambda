#ifndef DLAMBDA_TYPE_TRAITS_IS_UNION_HPP
#define DLAMBDA_TYPE_TRAITS_IS_UNION_HPP

#include <dlambda/type.hpp>

namespace dlambda {
  namespace type_traits {
    bool is_union( const type& );
  }
}

#endif

